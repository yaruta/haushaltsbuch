/**
 * This is the component to output a default error message in case of incorrect input to the input form.
 */

const ErrorMessage = () => {
  return <div>
    <p>Bitte füllen Sie alle Felder aus.</p>
  </div>;
};

export default ErrorMessage;
